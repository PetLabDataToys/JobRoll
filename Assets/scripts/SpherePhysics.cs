using UnityEngine;
using System.Collections;

public class SpherePhysics : MonoBehaviour { // TODO: re-write physics for ball so we don't have to use colliders

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
